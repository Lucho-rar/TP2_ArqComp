`timescale 1ns / 1ps

module top#(
    parameter D_BIT = 8,
    parameter OP_BITS = 6,
    parameter FREQ = 50E6,
    parameter BAUD_RATE = 9600,
    parameter SAMPLE_TIME = 16
)


(
    
    );
endmodule
